# morphic pulls cmdk@1.0.0 (peer react ^18) while the app is on react 19, so npm's strict
# peer resolution errors out (ERESOLVE). Vercel/CI run `npm install`/`npm ci` without flags,
# so pin the legacy resolver here — the same thing we pass locally as --legacy-peer-deps.
legacy-peer-deps=true
